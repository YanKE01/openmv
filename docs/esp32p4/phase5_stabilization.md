# Phase 5 — 稳定化与提交收尾

**状态：待开始**

## 目标

确保 ESP32P4 端口质量达到可合入主线的标准，完成文档、CI 和提交整理。

## 前置条件

- Phase 4（外设补全）完成

## 任务列表

### 构建回归

- [ ] 确认 `make TARGET=ESP32_GENERIC_P4` 在干净环境下可复现
- [ ] 确认原有 STM32/RP2/MIMXRT 等目标的编译不受 ESP32 改动影响
- [ ] 评估是否在 CI 中增加 ESP32P4 构建任务

### MicroPython 子模块

- [ ] 决定是否更新 `lib/micropython/ports/esp32/lockfiles/dependencies.lock.esp32p4` 中的 ESP-IDF 版本（当前记录 5.5.1，本地使用 5.5.3）
- [ ] 确认子模块 commit 是否需要切换到上游稳定版本

### 文档

- [ ] 在 `docs/boards.md` 中补充 `ESP32_GENERIC_P4` 板型条目
- [ ] 完善本目录下各阶段文档，补充已知限制和已知可用传感器列表
- [ ] 输出烧录步骤（面向终端用户）

### 提交整理

- [ ] 将开发过程中的调试性提交整理为若干语义明确的提交
- [ ] 确认 `plan.md`（本目录）中各阶段状态字段更新为最终状态
- [ ] 删除临时调试文件和注释

### 已知限制记录

在 `docs/esp32p4/plan.md` 中补充：

- 当前支持的传感器型号
- 当前不支持的 OpenMV 功能（及原因）
- 已知的性能限制（最大分辨率、帧率）

## 验收标准

- 在干净 Docker 或 CI 环境中 `make TARGET=ESP32_GENERIC_P4` 成功
- 原有目标编译不回退
- 文档、提交信息、已知限制均已整理完毕
